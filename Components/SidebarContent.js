import React from 'react'
import SearchedCard from './SearchedCard'

const SidebarContent = ({searchedData}) => {
  return (
    <SearchedCard
    
      searchedData={searchedData}
     />
  )
}

export default SidebarContent